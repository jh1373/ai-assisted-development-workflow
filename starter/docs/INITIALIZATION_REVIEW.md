# Initialization Review

Initialization Decision: Not Ready

このファイルは、通常タスクへ進む前の初期設定レビューに使います。
`templates/initialization-review.md` をもとに記入してください。

ユーザーの明示承認前に、`.ai-workflow/project-state.conf` を `ready` に変更してはいけません。
