# 初回設定状態の表示改善 計画

## 目的

プロジェクト案内図の`準備中`という曖昧な状態表示を、何を行っている段階なのか分かる`初回設定中`へ変更する。

## 対象

- localhost画面
- 自動生成される`DIRECTORY_MAP.md`
- 初回設定判定と構成検査テスト

## 完了条件

- `provisional`状態が`初回設定中`と表示される
- 旧表記`準備中`を使用する既存導入先も判定できる
- rootとstarterが同期し、全テストが成功する
